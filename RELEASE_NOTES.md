# MediaSorter 发布说明

按拍摄时间整理照片/视频,零依赖、跨平台、支持拖放。

## v1.2.2 更新内容

### 重构与体验优化

- **移除树形预览窗口**：预览完成后不再弹出按 年/月/日 目录树的预览窗口，改为在状态栏展示统计、日志写入 `logs/` 目录，避免弹窗干扰；预览仍为 DryRun 零风险模式，确认无误后再点『开始整理』正式执行
- **同步精简相关代码**：移除预览窗口相关的树形数据收集与 `showPreviewWindow` 弹窗逻辑，主流程更简洁、构建产物更小

## v1.2.1 更新内容

### 修复与体验优化

- **优化主窗口尺寸与布局**：窗口从 820x720 缩小到 700x440,适配精简后的三步走主流程,消除 UI 精简后遗留的大片空白;用 Border 布局固定底部进度/状态区、主内容区可滚动,源/目标文件夹 label 独立一行、输入框与按钮并排,主按钮行居中显示,状态文本支持自动换行
- **修复预览树形窗口空白/乱码问题**：将树形窗口里的非 ASCII 字形(📁/← 等)替换为 ASCII 字符(以 `[+]`/`<-` 表示),避免在部分系统(尤其 Windows 默认字体)渲染成乱码/黑块;并通过 `fyne.Do` 把「展开全部分支」推迟到窗口完成首次布局后再执行,解决 Fyne Tree 惰性加载导致树形区域一片空白的问题

## v1.2.0 更新内容

### 新功能

- **目录结构多种模板(GUI 下拉 + CLI `--layout`)**：参考常见整理工具,目录结构可下拉选择多种模板,也支持 CLI 自定义布局(用 Go 时间格式符 `2006`年/`01`月/`02`日,`{dir}` 按源目录名、`{flat}` 根目录平铺)。模板包括:
  - 根目录平铺(文件名带时间戳)/ `YYYY` / `YYYY-MM` / `YYYY/MM` / `YYYY/YYYY-MM` / `YYYY/MM/DD` / `YYYY/YYYY-MM-DD` / `YYYY-MM-DD` / 原目录名
- **文件名格式 5 种模板(GUI 下拉 + CLI `--name`)**：把「文件名处理(Separator)」从旧的 KeepOriginal 布尔扩展为 5 种命名模板,与目录结构模板设计一致:
  - `YYYY-MM-DD_HHMMSS`(默认) / `YYYY-MM-DD_HHMMSS_XXX`(始终带序号) / `YYYY-MM-DD_HHMMSS 原始名` / `YYYY-MM-DD_原始名` / `原始名`,可再加前后缀
  - CLI 支持 `--name=模板`(优先级高于 `--keep-original`)与 `--keep-original`,占位符:`{ts}`=`YYYY-MM-DD_HHMMSS`、`{date}`=`YYYY-MM-DD`、`{orig}`=原始文件名、`{seq}`=序号(_001,含它则始终带号保证唯一;不含则仅同名冲突时追加序号)
- **主窗口精简为三步走核心流程**：默认界面聚焦 选择源目录 → 处理 → 预览 三步,处理格式等进阶设置收进『高级选项』,界面更清爽

### 质量加固

- 新增 `NameLayout` 各模板单元测试 + `{seq}`/`{orig}` 端到端用例
- 目录结构多模板相关单测/边界用例补充

## v1.1.1 更新内容

### 新功能

- **日志落盘**：完整运行日志不再刷屏 UI,改为写入目标目录 `logs/mediasorter-<时间戳>.log`(便于留存与排查),并新增「打开日志」按钮,一键用系统默认编辑器查看
- **树形预览窗口**：预览完成后弹出按 年/月/日 目录树结构的预览窗口,展示每个文件的源 -> 目标映射(文件名后 ← 源文件),直观核对整理结果
  - 修复同名文件+同名目录时文件在树中丢失的问题
  - 预览 0 个文件时不弹空树窗口
- **处理格式 UI 参考图重构**：格式选择界面按 图片/视频 分组,并支持自定义格式

### 修复与体验优化

- **优化日志区显示**：调整日志区尺寸与换行
- **修复文件名前后缀输入框**：过小问题修复并补充说明
- **修复树预览与日志按钮问题**：新一轮运行时重置日志路径后同步禁用「打开日志」按钮,避免误弹『暂无日志文件』

### 质量加固

- 为 `OnPreview` 预览回调补充单元测试(验证 DryRun 回调 / 非 DryRun 不回调)

## v1.1.0 更新内容

### 修复与体验优化

- **移动选项移入『高级选项』折叠区**：危险操作(移动文件会删除源文件)默认收起,避免误触;点击展开/收起按钮时动画切换
- **修复 GUI 多个 UI 瑕疵**：窗口布局、控件状态等细节打磨
- **修复默认输出目录与跨盘移动 bug**：结果目录在跨磁盘时也能正确生成
- **修复运行结束按钮状态**：关闭日志通道后恢复按钮可用并输出最终状态
- **按钮灰化兜底**：防止运行期间按钮状态异常
- **修复构建问题**：修复 GUI 编译报错 `undefined: advancedBtn`(闭包作用域),恢复 Linux/Windows GUI 可构建
- **修复构建问题**：修复 GUI 编译报错 `undefined: advancedContent`(同源闭包作用域),确保 Windows GUI 可构建

### 质量加固

- **新增端到端测试**：覆盖扫描/去重/处理全流程
- **CI 加固**：`go test -race` 并发竞争检测 + 覆盖率统计 + JPEG/MP4/HEIF/TIFF fuzz 种子测试
- 单测/边界测试体系化,提升回归保障

## 主要功能

- **拍摄时间四级兜底**: 照片 EXIF(JPEG/HEIC) -> 视频 MP4 元数据 -> 文件名时间戳 -> 文件时间
- **规范命名**: `YYYY-MM-DD_HHMMSS_序号`,字典序 = 时间序
- **内容去重**: 按大小分组 + MD5,多 GB 视频不吃性能
- **格式选择**: 可只整理指定格式(JPG/HEIC/MP4 等)
- **自定义命名**: 保留原始文件名,或加前缀/后缀
- **录制日期筛选**: 只处理有/无录制日期的文件
- **预览模式**: 零风险查看整理结果,预览后按 年/月/日 树形窗口直观核对源 -> 目标映射
- **日志落盘**: 完整日志写入 `logs/mediasorter-<时间戳>.log`,一键打开查看
- **设置自动保存**: 记住上次的路径与选项
- **安全默认**: 只复制、冲突自动加序号、源文件永不删除

## 产物

| 文件 | 说明 |
|---|---|
| `mediasort` / `mediasort.exe` | 命令行版(Linux / Windows / macOS) |
| `mediasort-gui` / `mediasort-gui.exe` | 图形界面版(Linux / Windows) |
